{
  "name": "pixelava-v2",
  "version": "1.0.0",
  "main": "index.js",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "lint": "solhint contracts/**/*.sol",
    "prepare": "husky install",
    "start:local": "hardhat node --network hardhat",
    "console:local": "hardhat console --network local",
    "deploy:local": "hardhat deploy  --network hardhat",
    "deploy:testnet": "hardhat deploy --network alfajores",
    "verify:testnet": "hardhat sourcify --network alfajores",
    "deploy": "hardhat deploy --network celo",
    "verify": "hardhat sourcify --network celo"
  },
  "devDependencies": {
    "@celo/contractkit": "^1.3.0",
    "@nomiclabs/hardhat-ethers": "npm:hardhat-deploy-ethers",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "chai": "^4.3.4",
    "dotenv": "^12.0.4",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.4.7",
    "hardhat": "^2.6.6",
    "hardhat-deploy": "^0.9.24",
    "husky": "^7.0.2",
    "prettier": "^2.4.1",
    "prettier-plugin-solidity": "^1.0.0-beta.18",
    "solhint": "^3.3.6"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.5.0"
  }
}
